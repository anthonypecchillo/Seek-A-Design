import React from 'react';

const Always = () => <p>I'm a paragraph that always renders, despite what route we're in, because I do not have a defined <code>path</code> prop! I would be great for navbars and footers!</p>;

export default Always;